<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/wishlistTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:fontFamily="cursive"
        android:text="Wishlist"
        android:textColor="@color/Black"
        android:textSize="30dp"
        android:visibility="gone" />

    <ListView
        android:id="@+id/listView_products"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/wishlistTitleTextView"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:visibility="gone" />


    <TextView
        android:id="@+id/wishlistNoItemTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_margin="10dp"
        android:text="No Item in your Wishlist"
        android:textColor="@color/Gray"
        android:textSize="18dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/goShoppingButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/wishlistNoItemTitleTextView"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/basic_black_button"
        android:text="Go Shopping"
        android:visibility="gone" />

</RelativeLayout>

<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.shopinggururegister">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:debuggable="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.android.shopinggururegister.Activities.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".Activities.WelcomeActivity" />
        <activity
            android:name=".Activities.MainActivityNew"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.PasswordResetActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.ProductsListActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.SearchResultActivity" />
        <activity android:name=".Activities.VariationsActivity" />
        <activity android:name=".Activities.BuyNowActivity" />
        <activity android:name=".Activities.OrderdetailsActivity" />


    </application>

</manifest>